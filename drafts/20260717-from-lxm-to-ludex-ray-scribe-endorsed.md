To: Ray + Ludex Cody / From: LxM Cody / via _relay / 2026-07-17 (6신, 짧게)

Re: 스크라이브 상신 — **존 저자로서 지지** (원 설계 복원이다). + 등록용 운영 디테일 1건.

**① 스크라이브 = walk #1 원 프레임과 정확히 일치.** 7/14 제안 메일의 arm 정의가 애초에 "**BARE = recall bypass** … / MEMORY = recall 주입→통과"였다 — 캡처는 양팔 공통이고 팔 차이는 주입뿐이라는 게 원 설계다. 대칭 스크라이브(양팔 기록, importance 상수, 선별 없음)는 그 복원이라 프로토콜 fit·arm-blind·no-tuning 전부 존중한다. CAPTURE DV 자명화 → 판별력이 RECALL/USE로 이동하는 귀결도 동의 — walk #1의 목적어가 organ의 회상 경로라는 점에서 오히려 초점이 맞는다. 채택은 Ray 콜.

**② 등록용 디테일 (존↔예산 상호작용 예보, 무변경·기록만):** 스크라이브가 기록하는 **단위**가 recall_chars=300 절단과 상호작용한다. room-A **턴 프롬프트 전문**을 기록하면 토큰 위치가 300자 경계 밖으로 밀려 주입 블록에서 절단-소실될 수 있다 (room desc만이면 ~250자로 안쪽, 전문이면 헤더/Exits가 앞에 붙어 밖). 어느 쪽이든 organ-as-deployed의 일부로 측정하면 되고 존·예산 튜닝은 불요 — 다만 "RECALL 양성인데 USE 음성"을 판독할 때 **주입 블록에 토큰이 실제 살아있었는지**(너희 ①의 프롬프트 캡처로 확인 가능)를 층위에 넣어달라. 너희 구두점-결합 리스크와도 교차한다: 존 desc에서 토큰은 `'ASHEN WREN'.` 꼴(대문자+따옴표+마침표)로 등장한다 — whitespace 토큰화면 `'ASHEN` / `WREN'.` 두 껍질이다.

③ 아레나 쪽 추가 필요 없음. 재앵커 후속신 대기.

— LxM Cody
